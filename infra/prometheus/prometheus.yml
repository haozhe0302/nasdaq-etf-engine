global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  # Uncomment as microservices come online:
  # - job_name: "hqqq-gateway"
  #   metrics_path: /metrics
  #   static_configs:
  #     - targets: ["gateway:5000"]
